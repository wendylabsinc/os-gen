--- stage2.orig/rootfs/etc/rc.local
+++ stage2/rootfs/etc/rc.local
@@ -11,6 +11,10 @@
 #
 # By default this script does nothing.

+# Generate UUID if needed and update DNS service
+/usr/local/sbin/generate-uuid.sh
+/usr/local/sbin/update-dns-service.sh
+
 # Print the IP address
 _IP=$(hostname -I) || true
 if [ "$_IP" ]; then